{
  "name": "koubo-web",
  "private": true,
  "version": "0.1.0",
  "main": "electron/main.cjs",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "electron:dev": "concurrently -k -n api,vite,app -c green,cyan,yellow \"node scripts/ensure-api.cjs\" \"wait-on http-get://127.0.0.1:8777/api/health && vite\" \"wait-on http://127.0.0.1:5288 && electron .\"",
    "electron": "electron .",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "engine": "python ../packaging/prepare_engine.py",
    "pack:win": "npm run build && python ../packaging/prepare_engine.py --system win32 --arch x64 && electron-builder --win --x64",
    "pack:mac": "npm run build && python ../packaging/prepare_engine.py --system darwin --arch arm64 && electron-builder --mac --arm64",
    "pack:mac-x64": "npm run build && python ../packaging/prepare_engine.py --system darwin --arch x64 && electron-builder --mac --x64"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.6"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^9.1.2",
    "electron": "^33.4.0",
    "electron-builder": "^25.1.8",
    "typescript": "^5.6.3",
    "vite": "^6.0.3",
    "wait-on": "^8.0.3"
  }
}
